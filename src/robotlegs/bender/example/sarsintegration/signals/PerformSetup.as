package robotlegs.bender.example.sarsintegration.signals
{
	import org.osflash.signals.Signal;
	
	public class PerformSetup extends Signal
	{
		public function PerformSetup(...parameters)
		{
			super(parameters);
		}
	}
}